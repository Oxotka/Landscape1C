# ТЗ: Интерактивный ландшафт технологий 1С

> **Состояние на 05.09.2026:** ядро карты реализовано — 229 карточек, 24 категории, 6 блоков. Основная волна опроса уже идёт в Telegram и MAX; отсечка запланирована на 01.10.2026. Ближайший этап — завершение сбора, проверка данных и публикация «Итогов 2026» в октябре–ноябре. Публичная витрина пока показывает предварительный этап (247 участников).

## 1. Цель и видение

Создать **интерактивную карту** экосистемы 1С.

Главное отличие от обычного списка — **навигация и мнение**:
- фильтры по роли, контексту работы, зрелости, лицензии, происхождению и доступности на рынке РФ;
- карточки с описанием, ответом «зачем» и блоком «с чего начать» (сильная сторона исходного каталога, её сохраняем);
- связи между технологиями (аналоги и зависимости) и отдельная страница графа связей;
- для карточек с репозиторием — автоматические метрики (звёзды, дата последнего коммита) — *реализовано 13.07.2026*: `scripts/repostats.js` собирает по API GitHub/GitLab в статический `app/repostats.js` (запускается вручную, время от времени — как linkcheck), модалка показывает бейджи рядом с лицензией.

## 2. Источники вдохновения

| Проект | Ссылка | Что берём |
|---|---|---|
| CNCF Landscape | <https://landscape.cncf.io> | Визуальный эталон: карточки + фильтры + метки зрелости |
| Thoughtworks Tech Radar | <https://www.thoughtworks.com/radar> | Идея статусов Adopt/Trial/Assess/Hold |
| build-your-own-radar | <https://github.com/thoughtworks/build-your-own-radar> | Открытый генератор радара (если делать радар-вид) |
| roadmap.sh | <https://roadmap.sh> | Навигация по ролям, путь обучения |
| MAD Landscape | <https://mad.firstmark.com> | Пример нишевого ландшафта |
| OpenYellow | <https://openyellow.org> | Источник данных по 1С-OSS |

## 3. Функциональные требования

- **Карточки** технологий: логотип, краткое описание, метки.
- **Категории и блоки** — отправная структура из README дополнена и уточнена в ходе наполнения. Сейчас 24 категории (поле `categories` в `app/data.js`) сгруппированы в 6 тематических блоков (поле `blocks`):
  - **Проектирование**: Прототипирование · Диаграммы · Моделирование и архитектура · База знаний
  - **Разработка**: Разработка · Библиотеки и инструменты · Плагины и расширения · ИИ-ассистенты · Системы хранения версий · CI/CD
  - **Качество**: Автоматизированное тестирование · Статический анализ кода · Код-ревью
  - **Инфраструктура и эксплуатация**: Администрирование 1С · Сетевая инфраструктура и облако · Безопасность · СУБД · Мониторинг · Производительность и нагрузочное тестирование
  - **Данные и интеграции**: Интеграции · API и веб-сервисы · BI-системы
  - **Управление и компетенции**: Задачи · Сертификация
  - **Пороги ёмкости** (чтобы схема-постер читалась): подкатегория — не более **9** инструментов, раздел — не более **6** категорий. Превышение — повод разбить подкатегорию или завести новый раздел. Оба порога жёсткие, их проверяет `scripts/validate.js`; правила разметки — в [`METHODOLOGY.md`](METHODOLOGY.md) («Ёмкость подкатегорий и разделов»).
- **Фильтры и поиск** (две независимые оси — функция и контекст — плюс три скалярные метки и доступность):
  - **функциональная роль** (*что делаешь*): разработчик / администратор / аналитик / тестировщик (роль «администратор» включает devops/эксплуатацию);
  - **контекст работы** (*где/как работаешь*): франчайзи / инхаус / продукты / проекты (бывший «фриланс» влит во «франчайзи»);
  - **зрелость**: `базовое` / `продвинутое` / `нишевое` ;
  - **лицензия**: open-source / бесплатное / проприетарное;
  - **происхождение**: отечественное / зарубежное (импортозамещение);
  - **доступность** на рынке РФ: доступен / ограничен (отдельно от происхождения).
  Подробнее про поля см. METHODOLOGY.md
- **Деталь карточки** (модалка): описание, «зачем», «с чего начать», ссылки; для карточек с репозиторием — авто-метрики (звёзды, дата последнего коммита). Открытая карточка отражается в URL (`?tool=имя`) — дип-линки для шаринга работают на главной, графе и схеме.
- **Виды отображения**: ландшафт (карточки) + граф связей + схема-постер + путь освоения;
- **Embed** карточек на внешние сайты — *решили не делать (13.07.2026)*.

## 4. Модель данных

Данные живут в `app/data.js` — глобальный объект `window.LANDSCAPE` (`categories`, `blocks`, `axes`, `items`). Одна карточка из `items`:

```js
{
  name: "1C: Platform Tools",
  added: "2026-06-10",             // дата добавления в ландшафт (ISO); редактор ставит сам
  updated: "2026-06-27",           // дата последней правки карточки (ISO); редактор штампует при любом изменении
  category: "Разработка",          // одна из categories
  subcategory: null,
  description: "Расширение VSCode: инструменты разработки 1С в одной панели",
  why: "Не переключаться между утилитами при разработке около-1С проектов",
  logo: "1c-platform-tools.svg",   // файл в app/logos/; null → заглушка «1С»
  homepage: "https://marketplace.visualstudio.com/items?itemName=yellow-hammer.1c-platform-tools",
  repo: "https://github.com/yellow-hammer/vscode-1c-platform-tools", // → авто-метрики (app/repostats.js)
  start: ["Marketplace: …", "Репозиторий: …"],   // блок «с чего начать»
  maturity: "продвинутое",         // базовое | продвинутое | нишевое
  roles: ["разработчик", "администратор"],        // функциональная роль (массив)
  contexts: ["инхаус", "продукты"],               // контекст: франчайзи|инхаус|продукты|проекты
  origin: "отечественное",         // отечественное | зарубежное
  license: "open-source",          // open-source | бесплатное | проприетарное
  availability: "доступен",        // доступен | ограничен (доступность на рынке РФ)
  analogs: ["…"],                  // взаимозаменяемые (симметрично)
  depends: ["…"],                  // «зависит от» (направленно)
  aliases: ["гит", "git hub"]      // скрытые поисковые синонимы (рус. транслитерации и пр.)
}
```

**Инвариант значений:** строковые метки в карточке должны точно совпадать со значениями в `axes.<ось>.values` — иначе фильтр не сработает. Целостность проверяет `scripts/validate.js`.

## 5. Архитектура

**Свой статический фронтенд** (HTML/CSS/JS без сборки и зависимостей), данные — `app/data.js`, деплой статикой на GitHub Pages. К этому решению пришли, отвергнув готовый движок (см. п.8–9).

## 6. Контекст работы: стартовые наборы

Контекст работы 1С-разработчика заметно меняет набор инструментов. Реализовано меткой-осью `contexts` на элементах:

| Контекст | Что в фокусе | Характерные инструменты |
|---|---|---|
| **Франчайзи** (+ фриланс) | много клиентских баз, типовые, обновления, быстрые доработки; минимум инфраструктуры | Конфигуратор, ИТС, расширения, многобазная обработка, обмены, портативные инструменты, Infostart Toolkit |
| **Инхаус** | одна компания, глубокая поддержка и интеграции | EDT+git, мониторинг (Zabbix/Prometheus), брокеры, S3, кластер |
| **Продукты** (тиражные решения) | релизы, совместимость версий, качество | git, CI/CD (Jenkins/GitLab), Vanessa/YAxUnit, SonarQube/BSL LS, OPM, публикация на Инфостарт |
| **Проекты** (внедрение) | методология, крупные внедрения, нагрузка | СППР, EnterpriseData/Шина, 1С:КИП/ЦКК, нагрузочное тестирование, документирование |

Это не жёсткие границы, а ориентир: один инструмент может попадать в несколько контекстов.

## 7. Принятые решения

- **Шкала зрелости**: своя — `базовое` / `продвинутое` / `нишевое` 
- **Язык интерфейса**: только русский.
- **Рубрика разметки** (`roles/contexts/maturity/...`): размечаем по методологии (см. METHODOLOGY.md)

## 8. Почему не готовый движок (landscape2)

Сначала пробовали [landscape2](https://github.com/cncf/landscape2) (движок CNCF Landscape: YAML → статика, авто-метрики GitHub). Прототип показал, что он не закрывает ключевую идею проекта:

- **Кастомные фильтры не поддерживаются.** Набор фильтров фиксирован (категория, зрелость, лицензия, организация, страна + общий `TAG`). Наши две оси (роль + контекст) чисто отфильтровать нельзя; кастомные поля движок принимает, но не использует ни в фильтрах, ни на карточках.
- **Нет локализации UI.** Обвязка интерфейса — на английском, что конфликтует с решением «только русский».

**Итог: от landscape2 отказались**, артефакты разведки (его `landscape.yml`/`settings.yml` и сборку) удалили из репозитория.

## 9. Архитектурное решение

**Делаем свой статический фронтенд.** Две оси фильтров (роль + контекст) + зрелость/происхождение/лицензия/доступность, полностью русский UI, связи и граф. HTML/CSS/JS без сборки и зависимостей, данные — `window.LANDSCAPE` в `app/data.js`. Деплой статикой на GitHub Pages.

## 10. Связи между технологиями (реализовано, включая граф)

Связи превращают каталог в карту и дают навигацию «что дальше».

**Реализовано.** Два типа связей, по образцу схемы из StackTechnologies1C (`img/stacktechnology.png`):
- **Аналоги** — взаимозаменяемые инструменты (`RabbitMQ ↔ Kafka`, `Конфигуратор ↔ EDT ↔ VSCode`, СУБД между собой).
- **Зависимости** — связанные по необходимости (`EDT — git`, `Хранилище — Конфигуратор`, плагины → их хосты, `Kibana/Logstash → Elasticsearch`).

**Модель (фактическая).** У элемента два поля-массива `analogs` и `depends` (имена связанных карточек):
- **`analogs` — симметричны**: прописываются на обоих концах. Группируются по близости («простые» и «сложные» внутри одной категории связываются раздельно, а не все-со-всеми).
- **`depends` — направленные («зависит от»)**: A → B означает «чтобы осмысленно работать с A, нужен и B», и так выстраивается цепочка (IDE → git → Хранилище). Прописываются только на исходном конце.

В детальной модалке (`detail.js`) — блоки «Аналоги»/«Зависимости» с квадратными кнопками (лого + название), клик открывает связанный инструмент. Сейчас связи проставлены у 182 карточек (аналоги) и 84 (зависимости) на 05.09.2026. Целостность связей проверяет `scripts/validate.js`.

**Граф связей (реализован).** Отдельная страница `graph.html` (`graph.js`): force-directed canvas-граф по `analogs`/`depends` с фильтрами по роли и зрелости, тёмной темой и поддержкой тач-жестов на узких экранах. Клик по узлу открывает ту же детальную модалку, что и на главной.

**Схема-постер (реализована).** Отдельная страница `scheme.html` (`scheme.js`) — статичное древо «блок → категория → карточки инструментов», целиком в одном инлайн-SVG. Отбор по роли/зрелости и скрытие блоков, клик по карточке открывает ту же модалку. Экспорт без зависимостей: SVG (вектор), PNG/PDF (через canvas), майндмап `.mm` (FreeMind). Назначение — выгрузка карты картинкой/вектором (в т.ч. для печати и Illustrator).

**Отличия от первоначального плана.** Свели 5 предложенных типов к двум (аналоги/зависимости) — этого достаточно для навигации и совпадает с авторской схемой. Сделана направленность зависимостей и полноценный граф. Редактирование связей в `editor.html` — реализовано (аналоги двусторонние, зависимости односторонние).

## 11. Слой компетенций и грейдов («Путь» реализован, слой грейдов заморожен)

**Вид «Путь» — реализован** (`path.html` + `path.js`): маршрут освоения инструментов для выбранной роли без матриц компетенций, на уже размеченных данных. Ступени — шкала зрелости (База → Мейнстрим → Специализация), внутри ступени группировка по блокам ландшафта; отметки «знаю» с прогрессом хранятся в localStorage (`pathKnown`), роль — в URL (`?role=…`). Клик по инструменту открывает общую модалку. Ограничение зафиксировано на странице: путь подсказывает «что учить», но не заменяет учебу.

**Полный слой грейдов (Junior/Middle/Senior) — заморожен.** Эксперимент проведен в ветке `feature/competencies` (итог — `89c810f`): для одной роли грейд практически совпадает со шкалой зрелости, отдельный слой не добавляет ценности поверх «Пути». Замысел (компетенция ↔ технологии, компетенция ↔ грейд, вид «Грейд») сохраняем на случай появления качественных матриц компетенций; код и разметку в main не вливаем. Исследование перенесено в main как документация — `docs/competencies/` (README с итогом, coverage.md, draft-grades.md, исходники матриц; 14.07.2026), кодовый эксперимент (разметка грейдов в `data.js`, ступени в `path.js`, `scripts/grades.js`) остается только в ветке. Туда же смотрит заготовка роадмапа обучения — `docs/roadmap/README.md` (исследование 15.06.2026, влито в main 14.07.2026).

## 12. Экспертность, контрибьюторы и методология (частично реализовано)

Ценность ландшафта — в мнении (зрелость, рекомендации, связи), а мнению нужна видимая экспертиза. Делаем ставку на коллективную экспертизу уважаемых в сообществе людей, а не одного автора.

**Согласие (обязательно, до публикации).** Фото, имя и контакты любого человека — только с явного разрешения. Ссылаться на публичные профили (Telegram, GitHub, Инфостарт), а не на личные контакты; аватар — только с согласия. Процесс: короткое согласование «как это будет выглядеть» + ссылка на черновик.

**Как показываем экспертность (вводим постепенно):**
1. **Экспертный совет** — *реализован* (`council.html`): страница с кураторами (фото, имя, био, область курирования) и автором проекта. Сейчас 8 кураторов (разработка ×2, аналитика ×2, тестирование ×2, администрирование, инфраструктура) + автор; фото лежат в `app/people/`. Расширяется по мере согласия новых людей.
2. **Контрибьюторы** — открытый список всех участников (как в awesome/CNCF), мотивирует вносить вклад. *Отложено на потом (13.07.2026).*
3. **Атрибуция оценок** — «категорию курировал X», «метку зрелости подтвердили …» (приём Thoughtworks Tech Radar). *Пока не делаем — кураторы и так видны на странице совета; вернёмся при необходимости (13.07.2026).*

**Страница «Методология»** — *реализована* (`methodology.html`): как принимаются решения, как размечены оси, как предложить правку. Прозрачность = доверие. Рабочая версия правил разметки осей (роль, контекст, зрелость, происхождение, лицензия, доступность) и сравнение с другими ландшафтами ведётся в [`METHODOLOGY.md`](METHODOLOGY.md).

**Модель данных:** `experts: [{ name, role, photo, links, areas }]`; опционально `curatedBy` у категорий/элементов. Реализация — статикой.

**Порядок:** прототип, связи, разделы «Экспертный совет» и «Методология» — сделаны. Дальше: согласовывать новых экспертов; список контрибьюторов — отложен.

## 13. Популярность у сообщества (волна 2026 идёт)

Помимо экспертного мнения — собирать **реальную распространённость** технологий: насколько они используются на практике. Это второй сигнал ценности (мнение экспертов + факт использования).

**Принятое решение и реализация.** Ежегодный опрос через ботов Telegram и MAX; ответы собираются на VPS, проверенные агрегаты публикуются статикой. Ранее рассматривали внешние формы и живые счётчики «Я использую» на карточках; выбран опрос волнами, живые счётчики не реализуются в текущем плане.

- **Шкала:** «работал» / «слышал» / «не знаю». После «работал» — необязательный вопрос «использовал бы снова?», после «слышал» — «хочешь попробовать?». Это даёт узнаваемость, использование, возврат и интерес.
- **Профиль:** роль, уровень и контекст работы. Очередь строится по роли и зрелости из `app/data.js`; контекст сохраняется для сегментации результатов, но не фильтрует очередь. Непройденные роли и дополнительные нишевые инструменты предлагаются по желанию.
- **Механика:** случайный порядок с вкраплениями нишевых инструментов, возможность прерваться, продолжить и исправить ответы. Общее ядро бота, Telegram через long polling, MAX через webhook; состояние сессий раздельное.
- **Хранение:** append-only журнал `answers.jsonl`, последний ответ респондента по инструменту побеждает. Идентификаторы в журнале хешируются с солью; Telegram и MAX используют разные пространства идентификаторов. Один человек может участвовать через обе платформы — межплатформенного объединения аккаунтов нет.
- **Сбор и публикация разделены:** основная волна идёт до запланированной отсечки 01.10.2026; после проверки публикуются агрегаты и аналитический материал. Предварительные июльские результаты уже доступны на сайте. Сегментация используется вместо взвешенного общего балла.
- **Публикуемые данные:** сейчас — агрегаты без идентификаторов; обезличенный сырой датасет запланирован вместе с итогами, после проверки состава публикуемых полей.

**Как это устроено у State of JS** (ориентир; проверено по [about](https://2024.stateofjs.com/en-US/about/) и [результатам 2024](https://2024.stateofjs.com/en-US/libraries/)):

- **Шкала двухмерная.** Шаг 1 — опыт: «не слышал» / «слышал» / «использовал» (наша шкала). Шаг 2, опциональный — сентимент: положительный / нейтральный / отрицательный; для использовавших это «использовал бы снова?», для слышавших — «хочу попробовать?». До 2023 была одна совмещённая шкала из 5 вариантов, разнесли на два шага ради простоты ответа.
- **Метрики из этих двух измерений:** узнаваемость (слышал+использовал), использование, интерес («хочу попробовать» среди неиспользовавших) и **retention** («использовал бы снова» среди использовавших) — самая ценная: отделяет популярное по инерции от того, что реально нравится.
- **Для бота:** сентимент вторым тапом почти бесплатен (ответил «работал» → «использовал бы снова? 👍/👎/пропустить») и даёт метрику качества, а не только распространённости. Взять.
- **Процесс:** все вопросы можно пропустить (~15–20 мин); окно сбора ~4 недели (2024: 13.11–10.12, 14 015 ответов, публикация 16.12) — та же модель «волна с отсечкой»; распространение — открытая ссылка (рассылка прошлогодним респондентам + соцсети), самоотбор честно признаётся в методологии; вопросы проектируются публично через GitHub-ишьюсы, костяк стабилен между годами ради сравнимости; данные и API открыты.
- **Порядок разделов:** роль/маршрутизирующие вопросы в начале, остальная демография в конце — чтобы не отпугивать на входе. Помимо инструментов спрашивают мнения об экосистеме (согласие с утверждениями, удовлетворённость), ресурсы (блоги/подкасты/курсы) и демографию для сегментации — кандидаты на «потом», не для первой волны.
- **Два слоя State of JS — фичи языка и библиотеки.** Мы в волне 2026 покрываем только второй слой (инструменты). Аналог «фич языка» для 1С — **механизмы платформы**: СКД, расширения, асинхронность, HTTP-сервисы, механизмы интеграции и т.п. — «что из возможностей платформы реально используете». **Кандидат на волну 2027** (записано 04.07.2026): отдельный блок вопросов про механизмы; в 2026 не берём — состав заморожен, лёгкость первой волны важнее широты. Часть механизмов уже есть карточками ландшафта (СКД, Конфигуратор — исключены из опроса как универсальные) — для блока механизмов их придётся спрашивать иначе, не «работал/слышал», а «используете ли осознанно».

**Аналоги и позиционирование** *(исследовано 04.07.2026)*. Прямые аналоги формата существуют и живут годами:

- **Stack Overflow Developer Survey** (с 2011, 49–70 тыс. ответов, ~314 технологий) — ближайший по метрикам: их «Admired / Desired» = наши retention и интерес.
- **CNCF Landscape + Annual Survey** — ближайший структурно: карта инструментов экосистемы + ежегодный опрос об использовании проектов с карты. Наш проект — по сути «CNCF Landscape для 1С» с опросом.
- **JetBrains State of Developer Ecosystem** (с 2017) и **State of JS/CSS/HTML** — методологические доноры (шкала, волны, отсечка).
- **В 1С — «Большой опрос сообщества» Инфостарта** (10 000+ участников): широкий социологический срез (удалёнка, доходы, процессы), инструменты — крупными мазками (категории, топ упоминаний). **Пер-инструментной карты популярности с метриками State of JS, привязанной к живому каталогу, в 1С нет — наш опрос первый.** Инфостарт — не конкурент, а комплемент; его 10 тысяч — ориентир потолка аудитории ниши.
- Общий паттерн всех аналогов: первый выпуск — одинокий срез, ценность динамики появляется со второй волны. Наш задел под динамику: поле `wave` в каждом ответе, замороженная шкала и имена инструментов между волнами.

**Статус и план волны 2026** *(актуализировано 05.09.2026; операционные детали — `bot/README.md`, пошаговый ранбук запуска — локальный файл у автора и копия на сервере бота)*:

- **Бот реализован и развёрнут**: @stateOf1c_bot, код в `bot/` (ноль зависимостей, long polling, systemd на VPS). Ответы — append-only журнал `answers.jsonl` (обезличенный: вместо аккаунта — солёный хеш), последний ответ по инструменту побеждает.
- **Тайминг волны**: предварительный этап прошёл в июле, основная волна открыта 29.08.2026 и сейчас идёт. Отсечка запланирована на 01.10.2026; до неё — привлечение участников и напоминание «последняя неделя», затем проверка данных в октябре и публикация «Итогов 2026» в октябре–ноябре. Июльские и основные результаты — отдельные этапы, при объединении нужно указывать источник.
- **Заморозка состава на волну**: с мягкого запуска карточки можно добавлять, но нельзя удалять и переименовывать (переименование рвёт связку ответов с карточкой).
- **Инструменты анализа готовы** (`bot/`): `report.js` — метрики State of JS по инструментам (retention/интерес считаются только от ответивших на второй вопрос, пропуск — не «нет»); `funnel.js` — здоровье волны (приток по дням, воронка ядра, темп, перекос по ролям); `aggregate.js` → `app/survey2026.js` — счетчики ячейками роль×уровень×контекст (перекрестные срезы честные, витрина складывает ячейки под отбор); `app/survey2026.html` — витрина на обвязке сайта, будущая страница «Итоги 2026». **Решение: витрина и отчет совмещаются** — интерактивная витрина уже живет на странице, отчет-лонгрид к публикации вырастет на ней же поверх. В витрине: отборы с семантикой фильтров сайта (мультивыбор, ИЛИ внутри оси / И между осями, пусто = все) стоят над цифрами — реагируют плитки, профиль респондентов, квадрант и таблица; две полосы распределения (первый вопрос работал/слышал/впервые вижу + второй снова бы/не снова/хочу попробовать/не хочу) с тултипами и гашением соседних сегментов при наведении; логотипы карточек в таблице; retention в UI везде переведен как «возврат». Витрина опубликована на **landscape1c.ru/survey2026**, с 14.07.2026 — публична: страница в sitemap/llms.txt (`PAGES` в `sitegen.js`), индексируется (noindex снят, добавлены OG/canonical/Метрика), на нее ведут ссылка из блока «Опрос сообщества» методологии и пункт «Опрос 2026» общего меню. Данные `app/survey2026.js` лежат в репозитории (агрегаты по сегментам, без ПДн). Старый тестовый адрес `/test_survey` удален (13.07.2026).
- **Промежуточный опрос завершен и опубликован (13.07.2026)** *(изначально назывался «тестовый прогон»; переименован 14.07.2026 — «промежуточный» позволяет публично встраивать итоги)*: 247 респондентов, итоги — пост-ретроспектива со ссылкой на витрину; выборка перекошена (79% разработчики, 55% инхаус, 92% опытные+эксперты) — урок для сентября: заранее договариваться с каналами тестировщиков/аналитиков/новичков. С 29.08.2026 `survey2026.html` совмещает посадочную боевой волны и «Итоги предварительного этапа»: единая ссылка ведёт к кнопкам Telegram/MAX, ниже остаётся интерактивная витрина. Показ результатов на карточках сайта **встроен в main (14.07.2026)**: полоса метрик на карточках доски и в модалке (фон — узнаваемость, заливка — использование, риска — «взяли бы снова»), лояльность аналогов на кнопках связей; агрегаты лениво из `app/survey2026.js` (`LandscapeUI.surveyOf`, порог 10 ответов на метрику). Слой описан блоком «Опрос сообщества» в методологии (`app/methodology.html`) со ссылкой на итоги.
- **Публикация итогов**: страница «Итоги 2026» на самом ландшафте + блок на карточках в общей модалке; порог «мало данных» — меньше 10 ответов в знаменателе конкретной метрики проценты не показываем; сегментация вместо взвешивания. Обезличенный сырой датасет решено открыть — через этот репозиторий.
- **Боевая волна открыта (29.08.2026)**: Telegram и MAX работают без `CLOSED`, тестовые ответы и сессии очищены, задана новая соль и отсечка `2026-10-01`. Баннер опроса на главной включен (`SURVEY_BANNER_ENABLED = true` в `app/app.js`): прямые кнопки ведут в боты, ссылка «Подробнее и предварительные итоги» — на посадочную. Для приглашения участников подготовлен `bot/notify.js`, работающий по сохраненному снимку июльских сессий (~274 чата; ключи `state.json` — настоящие chat_id, в отличие от обезличенного журнала). Факт отправки проверяется по операционным журналам, а не по наличию скрипта. Баннер и цепочка тултипов онбординга главной (`app/onboarding.js`, флаг `onboarding_done`) оба целятся в первый визит и могут показаться вместе — проверка их совместного вида остаётся задачей, если ещё не выполнена.
- **Охват боевой волны**: спрашиваются продвинутые и нишевые: в локальном каталоге на 05.09.2026 это 202 инструмента после исключений (74 продвинутых и 128 нишевых). Состав на сервере зависит от развёрнутой версии каталога. **Базовые (13) в волну не входят — они уже закрыты промежуточным опросом** (вся его колода строилась вокруг базовых, ~240 респондентов); при публикации «Итогов 2026» их метрики подмешиваются из июльских данных с пометкой источника. Сертификация исключена из опроса (категория «Сертификация» — не инструменты), универсальные базовые — в `bot/excluded.json` («не применимо» в итогах).
- **Бот в MAX реализован (август 2026)**: тот же опрос работает отдельным процессом через webhook, с общим ядром и раздельным состоянием сессий; бот прошёл модерацию и участвует в текущей волне 2026 вместе с Telegram. Архитектура и ограничения — в [`docs/superpowers/specs/2026-08-13-max-bot-design.md`](superpowers/specs/2026-08-13-max-bot-design.md).

## 14. Инструмент редактирования и разметки (реализован)

Править данные напрямую в `app/data.js` (бегать по JSON руками) неудобно и чревато ошибками: легко опечататься в значении метки, рассинхронить его с `axes.*.values`, забыть поле. Нужен **визуальный редактор разметки**.

Идея — тот же вид, что и основной ландшафт (сетка карточек), но в **режиме редактирования**:
- менять метки осей (`роль / контекст / зрелость / происхождение / лицензия`) кликами по чипам, а не текстом;
- редактировать `name / description / why / start / связи` в форме карточки;
- **валидация по инварианту значений**: метки выбираются только из допустимых `axes.*.values`, рассинхрон невозможен;
- добавление и удаление карточек.

Вывод — **экспорт обратно в формат `app/data.js`**, чтобы результат можно было закоммитить.

**Реализовано** (`app/editor.html` + `editor.js`): сетка карточек в режиме редактирования, метки осей кликами по чипам с валидацией по `axes.*.values`, добавление/удаление карточек. По кнопке «Сохранить» весь `window.LANDSCAPE` сериализуется обратно тем же 2-пробельным JSON и пишется прямо в `app/data.js` — для записи нужен лёгкий пишущий сервер `scripts/serve.py` (POST `/save`); без него «Сохранить» откатывается на скачивание копии. Дата `updated` файла (`LANDSCAPE.updated`) проставляется автоматически; кроме того, при любой правке карточки редактор штампует ей `updated` (для двусторонних аналогов — и парной), а новые карточки получают `updated` = дате создания. Пока поле только собирается, в UI не показывается — задел под историю изменений и сводку «что нового с даты X». При ручной правке `data.js` дату ставь сам; `validate.js` ловит карточки без корректной `updated`.

## 15. Открытые вопросы

- **Логотипы**: на 05.09.2026 у 7 карточек не задан `logo`, сайт показывает заглушку. Для бота отдельно не подготовлены изображения gRPC и Unica — эти карточки отправляются текстом.
- **Модель вклада** — *реализована и работает* (репозиторий публичный): issue-формы «Предложить инструмент» и «Исправить карточку» (`.github/ISSUE_TEMPLATE/`), критерии включения и правила PR в `CONTRIBUTING.md`, CI-валидация данных на PR (`.github/workflows/validate.yml`); на сайте — «Предложить инструмент» в меню/подвале и «Заметили неточность?» в модалке карточки (форма с подставленным именем).
- **Тексты карточек**: отложена сплошная ревизия `description` + `why` по всем карточкам (циркулярные «зачем», мусорное форматирование).
- **Порядок реализации**: связи (п.10) и экспертность (п.12) — сделаны, «Путь» (п.11) — реализован, слой грейдов (п.11) — заморожен, авто-метрики репозиториев (п.1, п.3) — сделаны. Опрос популярности (п.13) — промежуточный опрос опубликован (13.07.2026), слой на карточках и страница итогов встроены в сайт (14.07.2026); основная волна уже идёт. Дальше — завершить сбор к запланированной отсечке 01.10.2026, проверить данные, объединить этапы с пометкой источника, подготовить аналитический материал, обновить метрики и опубликовать обезличенный датасет.
