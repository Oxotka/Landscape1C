# Баннер опроса на главной

Issue: [#28](https://github.com/Oxotka/Landscape1C/issues/28) «Сделать баннер про опрос уже».

## Цель

Привлечь трафик с главной страницы ландшафта в боевую волну опроса «State of 1C» (боты Telegram и MAX), когда волна откроется в сентябре 2026.

## Контекст

Оба бота опроса (`@stateOf1c_bot` в Telegram, `StateOf1C_bot` в MAX) сейчас в режиме `CLOSED=1` — отвечают заглушкой «большая волна в сентябре» (см. `docs/TZ.md` п.13). Публичной ссылки на боты на сайте сейчас нет вообще. Публикация баннера должна совпасть с открытием волны, а не произойти раньше.

## Область действия

Только главная страница (`app/index.html`). Другие страницы (граф, схема, путь, методология) баннер не получают.

## Поведение

**Включение.** Константа `const SURVEY_BANNER_ENABLED = false;` в начале `app/app.js`. Пока `false` — баннер не рендерится в DOM вообще (не просто скрыт стилями). В день открытия волны — один коммит меняет `false` на `true` и пуш (автодеплой на `main`).

**Показ/закрытие.** Если флаг включён:
- баннер рендерится, если в `localStorage` нет ключа `survey_banner_dismissed`;
- клик по крестику скрывает баннер и ставит `localStorage.survey_banner_dismissed = "true"` — баннер больше не появится в этом браузере (постоянно, до конца волны и после — ключ не сбрасывается автоматически).

Паттерн аналогичен `app/onboarding.js` (`onboarding_done` в localStorage).

## Разметка и размещение

Новый блок `.survey-banner` в `app/index.html`, между `</header>` (masthead) и `<div class="body">`, на всю ширину страницы. По умолчанию с атрибутом `hidden`; `app.js` снимает `hidden` при выполнении условий показа.

```html
<div id="survey-banner" class="survey-banner" hidden>
    <p class="survey-banner__text">
        Большой опрос «Состояние ландшафта 1С 2026» — расскажите, какими
        инструментами пользуетесь
    </p>
    <div class="survey-banner__actions">
        <a href="https://t.me/stateOf1c_bot" target="_blank" rel="noopener"
            >Ответить в Telegram →</a
        >
        <a href="https://max.ru/se13951546_bot" target="_blank" rel="noopener"
            >Ответить в MAX →</a
        >
    </div>
    <button id="survey-banner-close" class="survey-banner__close" aria-label="Закрыть">✕</button>
</div>
```

Текст без эмодзи (на сайте эмодзи в видимом UI нигде не используются).

## Стиль

Тёмная контрастная плашка, отличная от белых/офф-вайт блоков сайта:
- `background: var(--ink); color: var(--paper);`, `border-bottom: 2px solid var(--ink)`;
- кнопки-ссылки — обводка `var(--paper)`, без заливки (в духе `.badge--ghost`), острые углы, без теней;
- крестик закрытия — как существующий `#reset`/`#reset2` (`✕`), но в цвет `var(--paper)`;
- на `≤720px` текст и кнопки в столбик, крестик остаётся в правом верхнем углу плашки.

Логика фильтрации/рендера доски (`app/app.js`) не затрагивается — баннер живёт независимо, добавляется отдельной функцией инициализации (например `initSurveyBanner()`), вызываемой при старте страницы.

## Что не делается

- Никакой синхронизации с реальным статусом `CLOSED` бота (нет API-запроса к боту) — переключение полностью ручное, одним флагом.
- Баннер не добавляется в бургер-меню (`nav.js`) как отдельный пункт — это не действие страницы, а промо-блок.
- Другие страницы сайта не получают баннер в этой итерации.
